apiVersion: v1
kind: Service
metadata:
  name: ai-prediction-api-service
spec:
  type: LoadBalancer
  selector:
    app: ai-prediction-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
